<h1>Users List</h1>
<%= link_to('Register Here', new_user_path) %>
<ul>
  <% @users.each do |user| %>
    <li>
    <%= link_to(user.first_name + ' ' + user.last_name, user_path(user))%>
  </li>
  <% end %>
</ul>
